/**
 * List of coupon enums
 *
 * @module CouponEnums
 * @category Enums
 *
 */

export enum CouponDiscountType {
  percentage = 'percentage',
  fixed = 'fixed',
  free_months = 'free_months',
}

export enum CouponDurationType {
  one_time = 'one_time',
  repeating = 'repeating',
  forever = 'forever',
}
